/** Shared threshold for chat auto-follow and “at bottom” UI (px from scroll bottom). */
export const CHAT_SCROLL_BOTTOM_THRESHOLD_PX = 32

/** Tighter near-bottom window while the agent streams (reduces scroll fighting). */
export const CHAT_SCROLL_AGENT_BUSY_THRESHOLD_PX = 12
