# shared/ui

Компоненты **[shadcn/ui](https://ui.shadcn.com/)** (Button, Input, Dialog, …).

Добавление: `npx shadcn@latest add <component>` с путём в эту папку.

Импорт: `@/shared/ui/button`. Не дублировать примитивы в `features/` или `pages/`.

См. `docs/UI.md`.
