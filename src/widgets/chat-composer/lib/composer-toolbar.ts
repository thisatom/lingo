import { sidebarMenuIconButtonClass } from '@/shared/lib/sidebar-filter-menu-styles'

/** Matches sidebar filter icon buttons (size-7 ghost). */
export const composerToolbarIconClass = sidebarMenuIconButtonClass

export const CHAT_MODE_LABELS = {
  text: 'Agent',
  conversation: 'Agent Speech'
} as const
