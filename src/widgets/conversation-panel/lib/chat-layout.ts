/** Bottom padding for scroll area above floating composer (synced from MainPage overlay). */
export const CHAT_BOTTOM_INSET = 'var(--lingo-chat-bottom-inset, 7rem)'

/** Extra scroll padding below the last turn (above composer clearance). */
export const CHAT_SCROLL_BOTTOM_EXTRA_PX = 20

/** Top gap between chat header chrome and first turn. */
export const CHAT_CONTENT_TOP_PADDING_CLASS = 'pt-4'
